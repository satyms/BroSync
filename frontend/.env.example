# BroSync Frontend Environment Variables
# Copy this file to .env and fill in values.

VITE_API_URL=http://localhost/api/v1
VITE_WS_URL=ws://localhost/ws

# ── Proxy targets (for vite dev server only) ──────────────────────────────────
# Local dev: leave unset (defaults to http://localhost:8000)
# Inside Docker Compose: set to Docker service name
# VITE_BACKEND_URL=http://web:8000
# VITE_WS_BACKEND_URL=ws://web:8000
